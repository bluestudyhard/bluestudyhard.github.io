/*#include <stdio.h>
#include <math.h>
int main()
{
    double y;
    int x, z;
    scanf("%d", &x);
    if (x == 0)
        y = (23.0/7) + 4;
    if (x < 0)
    {
        z = pow(x, 3) + 4;
        y = abs(z) * (23.0 / 7.0);
    }
    if (x <= 6)
        y = (log(16) / log(3)) * (log(x) / log(3));
    else
        y = 5.0 / 27.0 * (x * x + 4 * x - 6);
    printf("%.3lf", y);
}
//*/
#include<bits/stdc++.h>
using namespace std;
int main()
{
    printf("%f ",sqrt(5));
}

//a = n/100000;
//    b = n/10000%10;
 //   c = n/1000%10;
  //  d = n/100%10;
  //  e = n/10%10;
  //  f = n%10;